---
phase: 01-frontend-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - src/App.tsx
autonomous: false

must_haves:
  truths:
    - "User sees complete Lithuanian UI on page load"
    - "User can upload images and see previews"
    - "User can select avatar, scene, and style"
    - "Generate button is disabled until images are uploaded"
    - "Layout is responsive: stacked on mobile, side-by-side on desktop"
  artifacts:
    - path: "src/App.tsx"
      provides: "Main application with full layout"
      min_lines: 50
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useImageUpload.ts"
      via: "hook import and usage"
      pattern: "useImageUpload"
    - from: "src/App.tsx"
      to: "src/components/upload/ImageUploader.tsx"
      via: "component import"
      pattern: "ImageUploader"
    - from: "src/App.tsx"
      to: "src/components/upload/ImagePreviewGrid.tsx"
      via: "component import"
      pattern: "ImagePreviewGrid"
    - from: "src/App.tsx"
      to: "src/components/config/ConfigPanel.tsx"
      via: "component import"
      pattern: "ConfigPanel"
    - from: "src/App.tsx"
      to: "src/components/ui/Button.tsx"
      via: "component import"
      pattern: "Button"
---

<objective>
Integrate all components into App.tsx with responsive layout, connecting upload system, configuration panel, and generate button into a cohesive interface.

Purpose: Complete the frontend foundation by wiring all components together. The result is a fully functional UI that allows users to upload images, configure settings, and see the Generate button state change based on form validity.
Output: Complete App.tsx with responsive two-column layout (mobile-first), all state management, and generate button validation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-frontend-foundation/01-RESEARCH.md
@.planning/phases/01-frontend-foundation/01-01-SUMMARY.md
@.planning/phases/01-frontend-foundation/01-02-SUMMARY.md
@.planning/phases/01-frontend-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate all components into App.tsx with responsive layout</name>
  <files>
    - src/App.tsx
  </files>
  <action>
Create the complete App.tsx that integrates all components:

src/App.tsx:
```tsx
import { useState } from 'react';
import { useImageUpload } from './hooks/useImageUpload';
import { ImageUploader } from './components/upload/ImageUploader';
import { ImagePreviewGrid } from './components/upload/ImagePreviewGrid';
import { ConfigPanel } from './components/config/ConfigPanel';
import { Button } from './components/ui/Button';
import { UI_TEXT } from './constants/ui';
import type { Config } from './types';

function App() {
  // Image upload state
  const {
    images,
    addImages,
    removeImage,
    canAddMore,
    hasImages
  } = useImageUpload();

  // Configuration state
  const [config, setConfig] = useState<Config>({
    avatar: null,
    scene: null,
    style: null
  });

  // Form validation
  const isConfigComplete = config.avatar && config.scene && config.style;
  const canGenerate = hasImages && isConfigComplete;

  // Generate handler (placeholder for Phase 3)
  const handleGenerate = () => {
    if (!canGenerate) return;

    // TODO: Phase 3 - Send to n8n backend
    console.log('Generate clicked', {
      images: images.map(img => img.file.name),
      config: {
        avatar: config.avatar?.id,
        scene: config.scene?.id,
        style: config.style?.id
      }
    });
  };

  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-50 to-gray-100">
      {/* Header */}
      <header className="bg-white shadow-sm">
        <div className="max-w-5xl mx-auto px-4 py-6">
          <h1 className="text-2xl md:text-3xl font-bold text-gray-900">
            {UI_TEXT.header.title}
          </h1>
          <p className="mt-1 text-gray-600">
            {UI_TEXT.header.subtitle}
          </p>
        </div>
      </header>

      {/* Main Content */}
      <main className="max-w-5xl mx-auto px-4 py-8">
        {/* Responsive two-column layout: stacked on mobile, side-by-side on md+ */}
        <div className="flex flex-col md:flex-row gap-8">

          {/* Left Column: Upload */}
          <div className="w-full md:w-1/2">
            <div className="bg-white rounded-xl shadow-sm p-6">
              <h2 className="text-lg font-semibold text-gray-900 mb-4">
                {UI_TEXT.upload.title}
              </h2>

              <ImageUploader
                onFilesSelected={addImages}
                canAddMore={canAddMore}
              />

              <ImagePreviewGrid
                images={images}
                onRemove={removeImage}
              />

              {!hasImages && (
                <p className="mt-4 text-sm text-amber-600">
                  {UI_TEXT.validation.noImages}
                </p>
              )}
            </div>
          </div>

          {/* Right Column: Configuration */}
          <div className="w-full md:w-1/2">
            <div className="bg-white rounded-xl shadow-sm p-6">
              <ConfigPanel
                config={config}
                onConfigChange={setConfig}
              />
            </div>

            {/* Generate Button */}
            <div className="mt-6">
              <Button
                onClick={handleGenerate}
                disabled={!canGenerate}
                className="w-full text-lg py-4"
              >
                {UI_TEXT.actions.generate}
              </Button>

              {/* Validation Messages */}
              {!canGenerate && (
                <div className="mt-3 space-y-1">
                  {!hasImages && (
                    <p className="text-sm text-gray-500">• {UI_TEXT.validation.noImages}</p>
                  )}
                  {!config.avatar && (
                    <p className="text-sm text-gray-500">• {UI_TEXT.validation.noAvatar}</p>
                  )}
                  {!config.scene && (
                    <p className="text-sm text-gray-500">• {UI_TEXT.validation.noScene}</p>
                  )}
                  {!config.style && (
                    <p className="text-sm text-gray-500">• {UI_TEXT.validation.noStyle}</p>
                  )}
                </div>
              )}
            </div>
          </div>
        </div>
      </main>

      {/* Footer */}
      <footer className="mt-auto py-6 text-center text-sm text-gray-500">
        Virtual Clothing Model Generator &copy; 2025
      </footer>
    </div>
  );
}

export default App;
```

Key implementation details:
1. Uses useImageUpload hook for image state management
2. Uses useState for config state (avatar, scene, style)
3. canGenerate is true only when hasImages AND all config fields are set
4. Responsive layout: `flex flex-col md:flex-row` (stacked mobile, side-by-side desktop)
5. Each column is `w-full md:w-1/2` (full width mobile, half width desktop)
6. All text from UI_TEXT constants
7. Generate button disabled state based on canGenerate
8. Validation messages shown when form is incomplete
  </action>
  <verify>
1. Run `npm run dev` and verify:
   - Page loads with Lithuanian header
   - Upload area visible on left (or top on mobile)
   - Config panel visible on right (or bottom on mobile)
   - Generate button disabled initially
2. Test upload:
   - Click upload area, select image
   - Preview appears
   - Can remove image
3. Test config:
   - Select avatar - description appears
   - Select scene and style
4. Test generate button:
   - With image + all config: button enabled
   - Missing any: button disabled with validation messages
5. Test responsive:
   - Open DevTools, toggle device toolbar
   - Mobile: stacked layout
   - Desktop: side-by-side
  </verify>
  <done>
Complete App.tsx created with:
- Responsive two-column layout (stacked mobile, side-by-side desktop)
- Image upload with preview and remove
- Configuration panel with all 3 selects
- Generate button with proper disabled state
- Validation messages when form incomplete
- All Lithuanian UI text
- Ready for Phase 3 integration with n8n backend
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 Frontend Foundation:
- React + TypeScript + Tailwind CSS v4 project
- Image upload system (1-3 JPG/PNG images with preview)
- Configuration panel (avatar, scene, style dropdowns)
- Responsive layout (mobile + desktop)
- Lithuanian UI throughout
- Generate button validation
  </what-built>
  <how-to-verify>
1. Open browser at http://localhost:5173 (or whatever port Vite shows)

2. **Verify Lithuanian UI:**
   - Header shows "Virtualus Drabuziu Modelis"
   - All labels in Lithuanian

3. **Test Image Upload:**
   - Click upload area - file browser opens
   - Select 1-3 JPG or PNG images
   - Images appear as thumbnails with numbers (1/3, 2/3, etc.)
   - Hover over image - red X button appears
   - Click X - image removed

4. **Test Configuration:**
   - Click avatar dropdown - 4 options appear
   - Select one - description box turns blue with avatar info
   - Click scene dropdown - 3 options
   - Click style dropdown - 4 options

5. **Test Generate Button:**
   - With no images: button disabled, shows validation messages
   - Upload image: "Ikelkite bent viena nuotrauka" message disappears
   - Select all options: button becomes enabled (blue, clickable)
   - Click button: check browser console for log output

6. **Test Responsive:**
   - Open DevTools (F12) -> Toggle device toolbar (phone icon)
   - Switch to mobile view: upload and config should stack vertically
   - Switch to desktop: should be side-by-side
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
After completing both tasks:

1. Dev server runs without errors
2. All imports resolve correctly
3. App renders with proper layout
4. Image upload works end-to-end
5. Config selection works for all 3 fields
6. Generate button state correctly reflects form validity
7. Responsive breakpoints work correctly
8. All UI text is Lithuanian
</verification>

<success_criteria>
Phase 1 complete when:
- [x] User can open application and see Lithuanian UI
- [x] User can upload 1-3 JPG/PNG images with preview
- [x] User can select avatar, scene, style from dropdowns
- [x] Avatar description shows when avatar selected
- [x] Generate button enabled only when images + all config selected
- [x] Layout responsive on mobile and desktop
- [x] Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-foundation/01-04-SUMMARY.md`
</output>
